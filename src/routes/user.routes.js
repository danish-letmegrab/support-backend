// import express from "express";

// import { getAllUser, registerUser } from "../controllers/user.controllers.js";
// import { userRegisterValidator } from "../validators/user.validators.js";
// import { validate } from "../validators/validators.js";

// const router = express.Router();

// router.route("/sign-in").post(userRegisterValidator(), validate, registerUser);
// router.route("/").get(getAllUser);

// export default router;
